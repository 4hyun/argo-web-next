import React from "react"
import tw, { styled } from "twin.macro"

const Container = styled.div`
  ${tw`w-full bg-gray`}
`

const BlogMainPage = () => {
  return <Container>Blog Main Page</Container>
}

export default BlogMainPage
