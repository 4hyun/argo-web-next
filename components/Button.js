import styled from "styled-components";
import tw from "twin.macro";

const Button = styled.button`
  ${tw`bg-argo-blue-400 hover:bg-indigo-700 flex font-bold text-white`};
  font-family: Open sans;
  font-style: normal;
`;

export default Button;
